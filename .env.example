# Only required for fetching Astrometry.net solved images for fine-tuning
# and ground-truth validation. The trained model itself does not need this.
# Get a free key at https://nova.astrometry.net/api_help
ASTROMETRY_API_KEY=your_key_here
